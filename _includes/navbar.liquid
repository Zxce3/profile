<nav class="navbar bg-light-lm">

                    <div class="progress">
                        <div class="progress-bar progress-bar-animated" role="progressbar" style="width: 100%;" aria-valuenow="100"
                            aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
    <div class="navbar-text">
        <span>{%- if page.layout == 'post' -%}
            <i class="bi bi-book font-size-18"></i> Post
            {% else %}
            <i class="bi bi-{{ page.icon }}  font-size-18"></i> {{ page.title }}
        {%- endif -%}</span>
    </div>
    <div class="navbar-content ml-auto">
        <div class="dropdown with-arrow">
            <a class="text-monospace btn btn-rounded" data-toggle="dropdown" id="time">00:00:00</a>
            <div class="dropdown-menu dropdown-menu-center shadow-lg">
                <div class="dropdown-content">
                    {%- include calendar.liquid -%}
                </div>
            </div>
        </div>
        <script type="text/javascript">
            var time = document.getElementById("time");
            function showTime() {
                var d = new Date();
                time.innerHTML = ('0' + d.getHours()).slice(-2) + ":" + ('0' + d.getMinutes()).slice(-2) + ":" + ('0' + d.getSeconds()).slice(-2);
            }
            setInterval(showTime, 1000);
        </script>
    </div>
    <div class="navbar-content ml-auto">
        <div class="dropdown with-arrow">
        <a class="text-monospace btn bg-transparent border-0 shadow-0" data-toggle="dropdown" id="nav-link-dropdown-toggle">
            <i class="bi bi-person font-size-18"></i> Memet
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="nav-link-dropdown-toggle">
            <a href="#" class="dropdown-item">API builder</a>
            <a href="#" class="dropdown-item">Functions</a>
            <a onclick="zxce3.toggleDarkMode()" type="button" class="dropdown-item">
                <i class="bi bi-moon mr-5 hidden-dm" aria-hidden="true"></i>
                <i class="bi bi-moon-stars-fill mr-5 hidden-lm" aria-hidden="true"></i>
                Mode <span class="hidden-dm">Light</span><span class="hidden-lm">Dark</span>
            </a>
            <div class="dropdown-divider"></div>
            <div class="dropdown-content">
                <div class="progress-group">
                    <div class="progress">
                        <div class="progress-bar progress-bar-animated" role="progressbar" style="width: 100%;"
                            aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>